# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO
#
# When uploading crates to the registry Cargo will automatically
# "normalize" Cargo.toml files for maximal compatibility
# with all versions of Cargo and also rewrite `path` dependencies
# to registry (e.g., crates.io) dependencies.
#
# If you are reading this file be aware that the original Cargo.toml
# will likely look very different (and much more reasonable).
# See Cargo.toml.orig for the original contents.

[package]
edition = "2018"
name = "wallpaper-ng"
version = "0.1.1"
authors = [
    "David Población Criado <davidpblcrd@protonmail.com>",
    "reujab <reujab@gmail.com>",
]
build = false
autolib = false
autobins = false
autoexamples = false
autotests = false
autobenches = false
description = "Gets and sets the desktop wallpaper/background."
readme = "readme.md"
keywords = [
    "desktop",
    "wallpaper",
    "background",
]
categories = ["api-bindings"]
license = "Apache-2.0"
repository = "https://github.com/davidpob99/wallpaper-ng"

[features]
from_url = [
    "dirs",
    "reqwest",
]

[lib]
name = "wallpaper_ng"
path = "src/lib.rs"

[dependencies.dirs]
version = "1"
optional = true

[dependencies.reqwest]
version = "0.12"
features = [
    "blocking",
    "rustls-tls",
]
optional = true
default-features = false

[dependencies.thiserror]
version = "2"

[target.'cfg(all(unix, not(target_os = "macos")))'.dependencies.dirs]
version = "6"

[target.'cfg(all(unix, not(target_os = "macos")))'.dependencies.rust-ini]
version = "0.21"

[target."cfg(unix)".dependencies.enquote]
version = "1"

[target."cfg(windows)".dependencies.enquote]
version = "1"

[target."cfg(windows)".dependencies.winapi]
version = "0.3"
features = ["winuser"]

[target."cfg(windows)".dependencies.winreg]
version = "0.55"
